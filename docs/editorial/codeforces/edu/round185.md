## F. Build XOR on a Segment

首先考虑一个问题, k 至多是多少. 那我们至少得知道 x 能不能被构造出来, 判断显然可以用线性基完成. 从线性基的角度看, k 的大小显然小于等于线性基的大小. 于是我们知道 k 其实很小.

考虑将右端点不断右移, 我们可以定义 dp(i, j) 表示, 用 j 个数字表示 i, 最靠右需要的左端点是什么. 后面就比较显然了, 不赘述.